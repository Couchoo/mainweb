#!/bin/bash

echo "------------------------------------------"
echo "🛠️ Couchoo WS Fixer v1.2"
echo "------------------------------------------"

# 1. Install Go
echo "📦 Степен 1: Инсталиране на Go..."
sudo apt update && sudo apt install -y golang-go

# 2. Check Go version
go version

# 3. Build the server
echo "🏗️ Степен 2: Компилиране на брояча..."
cd /root/mainweb/cinema-ws
rm -f ws-server # Изтриваме стара версия ако има
go mod tidy
go build -o ws-server

if [ -f "ws-server" ]; then
    echo "✅ Компилирането е успешно!"
else
    echo "❌ ГРЕШКА: Сървърът не можа да се компилира!"
    exit 1
fi

# 4. Fix PM2
echo "💾 Степен 3: Рестартиране на PM2..."
pm2 delete WS 2>/dev/null
pm2 start ./ws-server --name "WS"
pm2 save

echo "------------------------------------------"
echo "🔍 Проверка на портовете (трябва да виждаш 8080):"
netstat -tulpen | grep 8080

echo "------------------------------------------"
echo "✅ ГОТОВО! Провери сайта след 10 секунди."
pm2 list
