# 🚀 План за Dedicated Сървър и Миграция (Enterprise Grade)

Настоящият документ описва архитектурата, системните изисквания и стъпките за прехвърляне на Movie Platform от локална среда към мощен нает сървър.

---

## 🏗️ 1. Хардуерни Спецификации (Ultimate Performance)
Това е конфигурация, която гарантира 1000% стабилност при хиляди едновременни потребители и тежка автоматизация на заден план.

| Компонент | Минимум за вашия мащаб | **Препоръка (1000% сигурност)** |
| :--- | :--- | :--- |
| **CPU** | 8 Cores / 16 Threads | **AMD EPYC™ 7003 / 9004 (24+ ядра)** |
| **RAM** | 64GB DDR4 | **128GB - 256GB DDR5 ECC** |
| **Storage** | 1TB NVMe SSD | **2 x 2TB NVMe Gen4/5 (DataCenter Grade) in RAID 1** |
| **Network** | 1 Gbps | **10 Gbps Unmetered Link** |
| **Backup** | Single Drive | **External NAS Storage / S3 Bucket** |

---

## 🐧 2. Операционна Система и Базов Софтуер
*   **OS:** **Ubuntu 22.04.4 LTS (Jammy Jellyfish)** - най-висока съвместимост и дългосрочна поддръжка.
*   **Runtime:** Node.js v20+ (LTS), Go 1.22+, Redis 7.x.
*   **Database:** MariaDB 10.11 (LTS) - по-бърза от стандартен MySQL при чести заявки.
*   **Process Manager:** PM2 (Cluster Mode) - за автоматично рестартиране и мониторинг.
*   **Reverse Proxy:** Caddy v2 (автоматичен SSL, по-бърз от Nginx за WebSockets).

---

## 🛠️ 3. Стъпки за Миграция (Step-by-Step)

### Фаза А: Подготовка на Сървъра
1.  **Hardening:** Настройка на `ufw` (които портове да са отворени), смяна на SSH порта, забрана на root login.
2.  **Драйвери:** Инсталиране на Google Chrome Dependencies за **Playwright** (скрепера):
    ```bash
    npx playwright install-deps
    ```
3.  **База данни:** Настройка на `innodb_buffer_pool_size` на 60-70% от RAM паметта.

### Фаза Б: Прехвърляне на Данните
1.  **Локално:** Експорт на базата:
    ```bash
    mysqldump -u root -p movie_platform > backup.sql
    ```
2.  **Transfer:** Качване чрез SCP/Rsync на сървъра.
3.  **Restore:** Импорт на сървъра в MariaDB.

### Фаза В: Пускане на Услугите
1.  **Next.js App:**
    ```bash
    npm install
    npm run build
    pm2 start npm --name "cinema-front" --name "FRONT" -- start -- -p 3000
    ```
2.  **Go WebSocket Server:**
    ```bash
    cd cinema-ws
    go build -o ws-server
    pm2 start ./ws-server --name "WS-SERVER"
    ```

### Фаза Г: Мрежа и CDN
1.  **Cloudflare:** Настройка на DNS рекордите към IP адреса на сървъра.
2.  **Proxy:** Подсигуряване на сайта през Cloudflare Proxy (Orange Cloud) за DDoS защита.
3.  **SSL:** Caddy автоматично ще генерира сертификати чрез Let's Encrypt.

---

## 📈 4. Мащабиране за Бъдещето (Геймификация)
*   **Redis Cache:** Всички нива, награди и daily rewards ще се обработват през Redis, за да не се товари основната база.
*   **Playwright Workers:** Благодарение на 24+ ядра, ще можем да проверяваме по 50 филма едновременно без забавяне на сайта.
*   **Log Management:** Използване на `pm2-logrotate`, за да не се препълва диска.

> [!IMPORTANT]
> При закупуване на сървър, винаги избирайте локация в **Германия (Hetzner/OVH)** или **България (Delta.bg)** за минимален пинг към българските потребители.

> [!TIP]
> Когато сте готови със закупуването, аз мога да генерирам скрипт за **Еднократна Инсталация**, който ще подготви всичко автоматично.
